<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_nav_graph"
    app:startDestination="@id/home_navigation">

    <!-- Home Tab -->
    <include android:id="@+id/home_navigation" app:graph="@navigation/home_navigation" />

    <!-- Workout Tab -->
    <include android:id="@+id/workout_navigation" app:graph="@navigation/workout_navigation" />

    <!-- Profile Tab -->
    <include android:id="@+id/profile_navigation" app:graph="@navigation/profile_navigation" />
</navigation>
