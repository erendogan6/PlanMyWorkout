<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/auth_navigation">

    <!-- Auth feature navigation -->
    <include app:graph="@navigation/auth_navigation" />

    <!-- Onboarding feature navigation -->
    <include app:graph="@navigation/onboarding_navigation" />

    <!-- Home feature navigation -->
    <include app:graph="@navigation/home_navigation" />

    <!-- Workout feature navigation -->
    <include app:graph="@navigation/workout_navigation" />

    <!-- Progress feature navigation -->
    <include app:graph="@navigation/progress_navigation" />

    <!-- Profile feature navigation -->
    <include app:graph="@navigation/profile_navigation" />

</navigation>
